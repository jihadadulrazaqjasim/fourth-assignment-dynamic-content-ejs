<%- include('includes/head.ejs') %>
<link rel="stylesheet"href="/css/users.css">
</head>
<body>

<main>
<h1>Hi I am in users</h1>
    <a id="add_button" type="submit" href="/">Add New+</a>

    <% if(users.length > 0) {%>
    <ul>
        <% users.forEach((product)=>{
        %>
        <li><%= product.username %></li>
        <% })  %>
    </ul>
    <% } else { %> <h1>No Users</h1> <% }%>
</main>

<%- include('includes/end.ejs') %>